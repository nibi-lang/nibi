
(:= version "0.0.0")
(:= authors ["bosley"])
(:= description "Networking extension module")
(:= licenses [
  "MIT"
])

(:= sources [
  "net.nibi"
])

# dylib omitted so we don't load it as a 
# standard module - cffi used to access (POC)

(:= post [
  "export.nibi"
])
