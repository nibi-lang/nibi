(use "io" "threads")

(fn busy [] [
  (loop (:= i 0) (< i 100000000000) (set i (+ i 1)) (nop))
  (<- 42)
])

(io::println "Starting...")

(:= tcell (threads::future (busy)))

(drop tcell)

(io::println "END")

# Basically the same as early termination
