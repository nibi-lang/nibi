(use "io" "threads")

(fn busy [] [
  (threads::sleep (threads::time::seconds 5000))
  (<- 42)
])

(io::println "Starting...")

(:= tcell (threads::future (busy)))

(drop tcell)

(io::println "END")

# Basically the same as early termination
