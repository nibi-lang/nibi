(def x {})

(assert (eq 0 (set-contains x 1)))

(assert (eq 1 (set-insert x 4)))
(assert (eq 1 (set-insert x 5)))
(assert (eq 1 (set-insert x 6)))

(assert (eq 1 (set-contains x 4)))
(assert (eq 1 (set-contains x 5)))
(assert (eq 1 (set-contains x 6)))

(assert (eq 1 (set-erase x 5)))

(assert (eq 0 (set-contains x 5)))

(put x "\n")
