(use "io")

(try 
  (assert_eq "foo" "bar")
  (io::println "Expected >>> " $e))

(try
  (assert_eq "foo" 10)
  (io::println "Expected >>> " $e))

(try
  (assert_eq 10 "foo")
  (io::println "Expected >>> " $e))

(try
  (assert_eq 3.14 3.141)
  (io::println "Expected >>> " $e))

(try
  (assert_eq 3.14 3)
  (io::println "Expected >>> " $e))

(try
  (assert_eq 10 11)
  (io::println "Expected >>> " $e))

(try
  (assert_neq "x" "x")
  (io::println "Expected >>> " $e))

(try
  (assert_neq 40 40)
  (io::println "Expected >>> " $e))

(try
  (assert_neq 3.14 3.14)
  (io::println "Expected >>> " $e))

(io::println "COMPLETE")
