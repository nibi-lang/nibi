# All files will be loaded and piled into
# an `env` matching the name of the folder
# that this file is contained in

(:= version "0.0.0")
(:= authors ["bosley"])
(:= description "Mathematics module")
(:= licenses [
  "MIT"
])

(:= sources [
  "math.nibi"
])

# Executed after module is imported in-order
(:= post [
  "export.nibi"
])